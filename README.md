### TL:DR
A dynamic one-pager website, displaying info about a github user via API calls.

#### Changeing the user
The user can be set via URL parameters ?user={UserName}.
Is defaults to the subdomain, so hosting it via GitHub pages will display the users information (if you use the default domain).
